var board_data = []; //盤面データ [x][y][{score,point,group,isLocked,isBase}]
var player_data = []; //プレイヤーの名前、各機種のグループ分け JSONから読み込む
var player_cost = []; //プレイヤーの残りコスト Interger

$(document).ready(function () {
    //ページ読み込み時の動作
});